spring:
  datasource:
    url: jdbc:h2:mem:omstest;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
oms:
  auth:
    secret: test-secret-for-unit-tests
    admin-password: admin123
  open:
    api-key: test-open-key
  integration:
    mock: true
